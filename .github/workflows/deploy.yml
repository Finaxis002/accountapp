name: Deploy to Vercel
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Vercel Deploy
        uses: amondnet/vercel-action@v20
        with:
          vercel-project-id: ${{ secrets.prj_wz8N0hx0JQA2VdwMzt8Mh3BwQsqn }}
